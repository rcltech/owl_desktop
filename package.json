{
  "name": "OWL",
  "version": "0.0.1",
  "description": "Desktop version for OWL by RC Tech Club",
  "main": "main.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "tsc -w",
    "start": "npm run build && electron ./dist/main.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^8.2.5",
    "typescript": "^3.8.3"
  }
}
